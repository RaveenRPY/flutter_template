enum Toast {
  success,
  failed,
  warning;

  const Toast();
}

enum DocType {
  BIRTH,
  MARRAIGE,
  DEATH,
  PROFILE_PIC,
  MEDICAL_BILL,
  MEDICAL_REPORT
}

enum DeviceOS { ANDROID, HUAWEI }

enum Flavor {QA, UAT, DEV, LIVE }
